{
  "name": "netlify-vless",
  "version": "1.0.0",
  "description": "A simple WebSocket proxy for Netlify Functions",
  "scripts": {
    "build": "mkdir -p public",
    "start": "netlify dev",
    "dev": "netlify dev"
  },
  "dependencies": {
    "ws": "^8.5.0"
  },
  "devDependencies": {
    "@netlify/functions": "^2.0.0"
  }
}
